<!DOCTYPE html>
<html>
  <body>
    <div>
      <%= @player1.name %> vs. <%= @player2.name %>
    </div>
    <div>
      <%= @player2.name %> <%= @player2.hp %>hp
    </div>
    <div>
      <form action="/attack">
        <input type="submit" value="Attack">
      </form>
    </div>
</body>
</html>
